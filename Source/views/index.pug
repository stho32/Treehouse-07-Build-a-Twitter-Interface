extends layout.pug

block content
  section#content
    if name
      h2 Welcome #{name}

      form(method="post", action="/logout")
        button(type="submit") Log out
      
      br
      a(href=`/cards`) Start learning
    else
      h2 Welcome student!
