extends layout.pug

block content
    section#content
      h2=  text
      if hint
        p
          i Hint: #{hint}
    a(href=`${id}?side=${sideToShow}`)= sideToShowDisplay
    span &nbsp;
    a(href=`${baseUrl}`) Next
    span &nbsp; &nbsp;
    a(href=`${baseUrl}/../`) Learned enough
    
